export class AccessToken {
    userId: number;
    username: string;
    pictureUri: string;
    isPrivate: Boolean;
    isActive: Boolean;
    refresh_token: string;
    access_token: string;
    expires_in: number;
    issued: string;
    expires: string;
    token_type: string;
}
