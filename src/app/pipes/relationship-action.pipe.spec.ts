import { RelationshipActionPipe } from './relationship-action.pipe';

describe('RelationshipActionPipe', () => {
  it('create an instance', () => {
    const pipe = new RelationshipActionPipe();
    expect(pipe).toBeTruthy();
  });
});
